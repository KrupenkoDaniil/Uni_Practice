/* Array input/output shared by both programs of lab 1, part II. */

#ifndef ARRAY_IO_H
#define ARRAY_IO_H

void array_read(int *a, int n);
void array_print(const char *title, const int *a, int n);

#endif